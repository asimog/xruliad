export const fontVariables = {
  display: "--font-display",
  body: "--font-ui",
  mono: "--font-mono"
} as const;

export const fontClassName = "hypermyths-fonts";

export const fontUsage = {
  display: "Use for product names, strong headings, and gamer/mythic moments.",
  body: "Use for readable product copy and app UI.",
  mono: "Use for wallets, code, market data, simulation IDs, and logs."
} as const;
