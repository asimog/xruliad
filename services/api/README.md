# Shared API Service

Shared backend/API boundary for user, wallet, market, quest, media, analytics, and integration APIs.

This first migration pass keeps production APIs inside their migrated apps. This service fails clearly until `DATABASE_URL` and downstream app contracts are configured.
