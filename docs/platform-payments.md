# Platform Payments

Platform payments use pay.sh with platform credentials only.

Used for web video, intelligence, ads, research, simulation, display, and premium platform features.

Implemented:

- `packages/platform-payments`
- `services/platform-payments-worker`

Required env uses `PLATFORM_PAYSH_*`. These values must never be mixed with `USER_PAYSH_*`.
