# Command Protocol

A Command is an executable or collaborative unit of work.

Implemented in `packages/command-protocol`:

- command creation
- command contribution
- command run/export types
- local execution intent export
- contribution receipts

Web can run and display commands. Trading execution is exported locally only.
