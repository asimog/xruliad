# x402 Integration

`packages/x402-discovery` provides the x402 discovery boundary.

Live paid x402 execution requires facilitator/provider setup. User-local x402 calls must remain local when connected to private strategy or trading workflows.
