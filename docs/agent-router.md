# Agent Router

Implemented in `packages/agent-router` and `services/agent-router`.

The router maps product/tool requests to agent-callable product APIs and marks local-only routes for trading/execution.
