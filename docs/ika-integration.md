# Ika Integration

Ika is hackathon-critical for local execution guardrails and dWallet-style signing/policy schemas.

Implemented:

- `packages/ika`

Current behavior is a typed boundary. Real policy enforcement requires Ika devnet/program env values. Local user approval remains mandatory.
