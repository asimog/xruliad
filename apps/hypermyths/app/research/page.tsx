import { TerminalRoutePage } from "../terminal-route-page";

export default function Page() {
  return <TerminalRoutePage title="Research" summary="CancerHawk and HyperKaon research tasks, safe summaries, dataset quests, and simulations." badges={["Web available", "Hybrid available"]} items={["Cancer research task", "Synthetic data quest", "Physics simulation", "Compute quest", "No treatment claims", "Research receipt"]} />;
}
