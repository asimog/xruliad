import { TerminalRoutePage } from "../terminal-route-page";

export default function Page() {
  return <TerminalRoutePage title="Inference" summary="Cheapest safe inference router for public commands with QVAC preferred for private reasoning." badges={["Web available", "Hybrid available", "Requires QVAC"]} items={["OpenRouter free", "OpenRouter paid", "x402", "pay.sh platform", "QVAC local", "Blocked key material"]} />;
}
