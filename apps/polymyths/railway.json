{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/polymyths/Dockerfile"
  },
  "deploy": {
    "numReplicas": 1,
    "healthcheckPath": "/api/health",
    "restartPolicy": {
      "maxRetries": 3
    }
  }
}
