# Supabase

Supabase stores shared web state and public/permissioned metadata. It must not store user trading keys, wallet private keys, local pay.sh private keys, or raw private strategy unless explicitly encrypted and opted in.
