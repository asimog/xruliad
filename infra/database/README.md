# Database

HyperMyths currently uses Prisma/PostgreSQL. Hypertian currently uses Supabase migrations. CancerHawk currently stores generated results and Python worker state.

Do not merge schemas until app builds and deployment paths are stable.
