# Cloudflare

Use Cloudflare for DNS, caching, security headers, tunnels, or workers where appropriate. Keep domain ownership product-specific and do not merge all public traffic into one frontend unless the product strategy changes.
