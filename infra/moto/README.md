# moto / fstack

The requested buildingopen/moto URL redirects to floomhq/moto, now described as fstack.

Set `MOTO_BASE_PATH` to a local clone/install path. Use `pnpm moto:check` to verify the boundary.
