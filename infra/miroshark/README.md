# MiroShark

MiroShark is integrated through `packages/simulation` as an external/local service boundary.

Set `MIROSHARK_BASE_URL` and `MIROSHARK_API_KEY`. If running locally, follow upstream MiroShark setup for Neo4j, Python, Node, and Docker/launcher requirements.
