# pay.sh

pay.sh is integrated through `packages/payments`.

Set canonical `PAYSH_*` variables and install the `pay` CLI. Use sandbox mode for development and tests. Live payments require local wallet authorization and real funded wallet setup.
