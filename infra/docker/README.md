# Docker

Docker is used for worker/service deployment and external integration dependencies. HyperMyths source Dockerfiles were preserved in `apps/hypermyths`. CancerHawk Railway/Nixpacks files were preserved in `apps/cancerhawk`.
