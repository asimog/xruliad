# Vercel

Deploy each app as a separate Vercel project with its own root directory:

- `apps/hypermyths`
- `apps/hypertian`
- `apps/cancerhawk`
- `apps/hyperkaon`
- `apps/polymyths`

Preserve app-specific `vercel.json` files where present.
